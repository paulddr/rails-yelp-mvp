<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.label :rating %>
  <%= f.number_field :rating %>
  <%= f.submit 'Leave review', class: 'btn btn-primary', placeholder: "0 - 5" %>
<% end %>
